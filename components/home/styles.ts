import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    input: {
        height: 40,
        margin: 8,
        borderWidth: 1,
        padding: 10,
        width: "100%"
    },
    container: {
        display: "flex",
        alignItems: "center",
        gap: 10
    }
});